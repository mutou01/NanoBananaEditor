# Image Generation Provider Configuration
# Supported providers: gemini, openai, custom
VITE_IMAGE_PROVIDER=openai

# =============================================================================
# Gemini Provider Configuration (default)
# =============================================================================
VITE_GEMINI_API_KEY=
VITE_GEMINI_MODEL=gemini-2.5-flash-image-preview

# =============================================================================
# OpenAI Image-to-Image Models Configuration
# For runtime model switching between gpt-image-2 and gpt-5.5
# =============================================================================

# gpt-image-2: Traditional OpenAI Image API (multipart/form-data)
VITE_GPT_IMAGE_2_API_URL=https://freeapi.dgbmc.top/v1/images/edits
VITE_GPT_IMAGE_2_API_KEY=sk-gz05xF8kN1Cq6LFLUy1UhDlTa0soVALwsAZ0lbUoCSzuoO2p

# gpt-5.5: OpenAI Responses API (JSON format)
VITE_GPT_55_API_URL=https://api.ppinfra.com/openai/v1/responses
VITE_GPT_55_API_KEY=sk_ErwvhZ4Z_0r5v5Btgqj2eCBY0WzPhjVJQ9RFulMza20

# =============================================================================
# Legacy OpenAI/Custom Provider Configuration (for backward compatibility)
# Use these when VITE_IMAGE_PROVIDER is set to 'openai' or 'custom'
# =============================================================================
VITE_IMAGE_API_URL=https://freeapi.dgbmc.top/v1/images/generations
VITE_IMAGE_API_KEY=sk-gz05xF8kN1Cq6LFLUy1UhDlTa0soVALwsAZ0lbUoCSzuoO2p
VITE_IMAGE_MODEL=gpt-image-2

# =============================================================================
# Default Generation Parameters (optional)
# =============================================================================
# Default image size: 800x800 (square), 600x800 (portrait), auto
# Note: 800x800 ensures generated images are under 2MB for e-commerce use
VITE_IMAGE_DEFAULT_SIZE=800x800
# Default temperature (0.0 - 1.0, affects creativity/quality tradeoff)
VITE_IMAGE_DEFAULT_TEMPERATURE=0.7
# Default number of images to generate per request
VITE_IMAGE_DEFAULT_N=1

# =============================================================================
# Notes
# =============================================================================
# - In production, API calls should go through a backend proxy
# - VITE_IMAGE_PROVIDER determines which provider to use:
#   * 'gemini': Use Google's Gemini 2.5 Flash Image model
#   * 'openai': Use OpenAI DALL-E or GPT-Image models
# - For image-to-image with model selection, the system uses:
#   * gpt-image-2: Traditional image editing API
#   * gpt-5.5: OpenAI Responses API with image capabilities
# - Do not commit actual API keys to version control
